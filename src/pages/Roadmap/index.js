import classNames from 'classnames/bind';
import styles from './Roadmap.module.scss';

function Roadmap() {
    return (
        <div>
            <h2>Roadmap page</h2>
        </div>
    );
}

export default Roadmap;
